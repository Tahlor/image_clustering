name: Clean temporary export branch

on:
  push:
    branches: [master]

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Delete temporary export branch
        run: git push origin --delete agent/export-current-master-artifact || true
